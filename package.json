{
  "name": "enthalpic-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Landing page — Enthalpic: verification infrastructure for marine carbon",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve .",
    "build": "echo 'Static site — no build step required.'"
  }
}
